# Data Layout

This repository keeps the extracted source dataset at the project root and generated artifacts under this directory.

Suggested usage:

- `raw`: optional links or metadata for original assets
- `interim`: temporary feature files
- `processed`: manifests and training-ready tables
- `external`: third-party resources
